CREATE DATABASE hello-world;

USE hello-world;

CREATE TABLE message (
        ID BIGINT(20) NOT NULL AUTO_INCREMENT,
        TEXT VARCHAR(255) NULL DEFAULT NULL,
        PRIMARY KEY (`ID`)
);

grant all privileges on hello_world.* to psi identified by 'psi_pwd';